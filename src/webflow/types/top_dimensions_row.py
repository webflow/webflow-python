# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class TopDimensionsRow(UniversalBaseModel):
    """
    A single value within the chosen dimension, with its count for the requested `metricScope`.
    """

    attribute_key: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="attributeKey"),
        pydantic.Field(
            alias="attributeKey",
            description="Identifier for the dimension value. Pass this back into a `filter` on any Analyze report to scope that report to this value.",
        ),
    ]
    name: str = pydantic.Field()
    """
    Display label for the dimension value. For many dimensions this matches `attributeKey`; for others (such as `region`, `trafficSource`, and `audienceIds`) it's a human-readable label.
    """

    count: int = pydantic.Field()
    """
    Non-negative count for this dimension value, in units of the request's `metricScope`.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
