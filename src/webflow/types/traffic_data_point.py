# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class TrafficDataPoint(UniversalBaseModel):
    """
    A single daily bucket in a traffic time series. Buckets are aligned to local day boundaries in the response's `bucketing.bucketTimeZone`; the `timestamp` is the UTC instant for that local bucket start.
    """

    timestamp: dt.datetime = pydantic.Field()
    """
    Start of the daily bucket, in ISO 8601 / RFC 3339 format.
    """

    count: int = pydantic.Field()
    """
    Non-negative count for this day, in units of the request's `metricScope`.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
